#include "server.h"

namespace Janus {

    void QuePaxaServer :: propose(int step, const Proposal& proposal){
        // Empty
    }
    SlotState QuePaxaServer :: processProposal(int step, const Proposal& proposal){
        // Empty
    }
}